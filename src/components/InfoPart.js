import * as React from 'react';
import styled from 'styled-components';

const InfoPart = styled.p({
  fontSize: '1em',
  fontWeight: 300,
  letterSpacing: '0.05em',
  margin: '0.5em',
});

export const InfoLink = styled.a({
  color: '#2196F3',
  textDecoration: 'none',
});

export default InfoPart;
